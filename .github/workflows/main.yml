on:
  push:
    branches:
      - hashish


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: "login to docker hub"
        run: docker login --username hashish1934 --password dckr_pat_RY8_iwQZ5JgQoAO0KRXRSzJp07w
